---
name: cluster_vm_count
runner_type: python-script
description: Return an object containing each cluster with the number of VMs on each one.
enabled: true
entry_point: cluster_vm_count.py
parameters:
    open_nebula:
        type: string
        description: >
            Pre-Configured Open Nebula connection details
        required: false
        default: ~
