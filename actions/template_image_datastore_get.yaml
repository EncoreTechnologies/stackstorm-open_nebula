---
name: template_image_datastore_get
runner_type: python-script
description: Retrieves the images on a template and returns the datastore(s) they are on
enabled: true
entry_point: template_image_datastore_get.py
parameters:
    template_id:
        type: integer
        description: ID of the Template
        required: true
    open_nebula:
        type: string
        description: >
            Pre-Configured ON connection details
        required: false
        default: ~
